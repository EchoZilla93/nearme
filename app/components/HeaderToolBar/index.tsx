import { SearchBar } from '@/app/components/HeaderToolBar/SearchBar';

const HeaderToolBar = () => {
  return (
    <header className='bg-blue-200'>
      <SearchBar />
    </header>
  );
};

export default HeaderToolBar;
